import GridLayout, { defaultProps } from './GridLayout';

export default GridLayout;
export { default as Block } from './Block';

export const cols = defaultProps.cols;
export const rowHeight = defaultProps.rowHeight;
export const margin = defaultProps.margin;
export const containerPadding = defaultProps.containerPadding;
